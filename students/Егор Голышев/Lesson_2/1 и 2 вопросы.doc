1. Дан код:
var a = 1, b = 1, c, d;

c = ++a; alert(c);           // 2 // оператор "++" это инкремент, он увеличивает переменную, к которой применен, на единицу. В префиксной форме (перед переменной) он возвращает увеличенное значение перменной "а"

d = b++; alert(d);           // 1 // этот же оператор в постфиксной форме (после переменной), возвращает исходное значение переменной "b", увеличенное значение будет использовано при следующем вызове перменной

c = (2+ ++a); alert(c);      // 5 // после первой операции перменная "а" равна 2, мы увеличиваем ее еще на 1 и прибавляем 2 ( получается 2+3)

d = (2+ b++); alert(d);      // 4 // аналогично предыдущему, только переменная "b" была равна 1, ее значение увеличилось при следующем вызове

alert(a);                    // 3 // после очередного увеличения перменная "a" равна 3

alert(b);                    // 3 // значение переменной "b" увеличилось при следующем вызове

Почему код даёт именно такие результаты?


2. Чему будет равен x в примере ниже?
var a = 2;
var x = 1 + (a *= 2); // ответ будет 5.